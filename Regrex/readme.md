# Regrex:(Regular Expression)
this is basically used for checking the pattern for matching of the string and manipulation used in the string text.
some of the code used in the researching in  the code .
*impoort re* we import the regular expression library
**These are the common used in the python code.**
re.match
re.search
re.findall()
re.spilt()
re.sub()
re.complie()